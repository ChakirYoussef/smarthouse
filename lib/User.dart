class User {
  final name;
  final lastName;
  final userName;
  final password;
  final pathImage;
  const User({required this.name,required this.lastName,required this.userName,required this.pathImage,required this.password});
}